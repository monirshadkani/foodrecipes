{% extends "base.html.twig" %}

{% block body %}
<div class="container mx-auto p-6">
    <h1 class="text-3xl font-semibold text-center mb-6">Ajouter un Nouvel Ingrédient</h1>

    {{ form_start(form) }}
        <div class="mb-4">
            {{ form_row(form.label, {'attr': {'class': 'block w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
        </div>
        <div class="mb-6">
            {{ form_row(form.unit, {'attr': {'class': 'block w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
        </div>
        <div class="text-center">
            <button type="submit" class="bg-blue-500 text-white px-6 py-3 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400">Ajouter l'ingrédient</button>
        </div>
    {{ form_end(form) }}

    <div class="mt-6 text-center">
        <a href="{{ path('ingredient_index') }}" class="text-blue-500 hover:text-blue-700">Retour à la liste des ingrédients</a>
    </div>
</div>
{% endblock %}
